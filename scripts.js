var sentence = prompt("Give me a sentence!");

var capitalizeLetters = function(string) {
 var firstLastLetter = str.charAt(str.length-1);
  var firstLastCap = firstLastLetter.upperCase();
  return capLastLetter;
};

alert(capitalizeLetters(sentence))
